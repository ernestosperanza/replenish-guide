---
import { Flex } from "webcoreui/astro";
import { Image } from "astro:assets";

export interface Props {
    title: string;
    image: string;
    collapsible?: boolean;
}
const { title, image, collapsible = false } = Astro.props;

const images = import.meta.glob<{ default: ImageMetadata }>(
    "/src/assets/cards/**/*.{jpeg,jpg,png,gif,webp}",
);
const fullPath = `/src/assets/cards/${image}`;

let importedImage: ImageMetadata | null = null;
if (images[fullPath]) {
    importedImage = (await images[fullPath]()).default;
}
---

{
    collapsible ? (
        <details style="margin-bottom: 0.5rem; border-bottom: 1px solid var(--w-color-primary-80); padding-bottom: 0.5rem;">
            <summary style="cursor: pointer; font-size: 1.25rem; font-weight: bold; margin-bottom: 0.5rem; color: var(--w-color-primary-10); padding: 0.5rem; border-radius: var(--w-sm-radius);">
                {title}
            </summary>
            <div style="padding-top: 0.5rem;">
                <Flex gap="lg" alignment={{ vertical: "start" }} wrap="wrap">
                    {importedImage ? (
                        <Image
                            src={importedImage}
                            alt={title}
                            width={400}
                            style="width: 220px; height: auto; border-radius: 4.75% / 3.5%; flex-shrink: 0; border: 1px solid var(--w-color-primary-50);"
                        />
                    ) : (
                        <img
                            src={fullPath}
                            alt={title}
                            style="width: 220px; border-radius: 4.75% / 3.5%; flex-shrink: 0; border: 1px solid var(--w-color-primary-50);"
                        />
                    )}
                    <div style="flex: 1; min-width: 250px;">
                        <slot />
                    </div>
                </Flex>
            </div>
        </details>
    ) : (
        <Flex
            gap="lg"
            alignment={{ vertical: "start" }}
            wrap="wrap"
            style="margin-bottom: 0.5rem;"
        >
            {importedImage ? (
                <Image
                    src={importedImage}
                    alt={title}
                    width={400}
                    style="width: 220px; height: auto; border-radius: 4.75% / 3.5%; flex-shrink: 0; border: 1px solid var(--w-color-primary-50);"
                />
            ) : (
                <img
                    src={fullPath}
                    alt={title}
                    style="width: 220px; border-radius: 4.75% / 3.5%; flex-shrink: 0; border: 1px solid var(--w-color-primary-50);"
                />
            )}
            <div style="flex: 1; min-width: 250px;">
                <h3 style="margin-top: 0; margin-bottom: 0.75rem;">{title}</h3>
                <slot />
            </div>
        </Flex>
    )
}
